command="$HOME/.config/i3blocks/lib/$BLOCK_NAME"
separator_block_width=15

[network]
interval=persist
markup=pango

[cpu-usage]
interval=persist
markup=pango

[memory-usage]
interval=10
markup=pango

[disk-usage]
interval=60
markup=pango

[volume]
interval=once
markup=pango
signal=1

[battery]
interval=persist
markup=pango

[time]
interval=60

[weather]
interval=3600
markup=pango
